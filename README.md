This is a python game that requires pygame to be installed on your machine.
Once you check out from the repository, make a subdirectory called "images", then move the two bmp files to the images directory.
